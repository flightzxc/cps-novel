<aside>
🗃️

**本页已于 2026-08-01 降为历史快照与 Owner 答案存档。** 正式任务管理已迁入「**🗂️ 技术调研任务台账**」数据库（32 行：W1–W9 / C1–C5 / I1–I10 / X1–X7 / E1），**任务状态以那里为唯一真源**，本页不再新增行。本页保留价值：你在表格末列手填的原始答案（W1–W11）与接口探测字段类别清单。

⚠️ **口径已修正**：原文「只有 W 道的前三条是 Gate 0，会卡住整个 P1」**已作废**。现行四级 Gate：**Gate A**（P1 本地开工，只需书量语种区间 + 五条架构基线）、**Gate B**（P1 生产门禁）、**Gate C**（P2 试读展示，阻于 W2/I6）、**Gate D**（P3 第一渠道接入，阻于 W1/I1–I10）。渠道未知项**只阻 Gate C/D，不阻 P1 本地开工**。

</aside>

本页原为立项书 §七待决问题与审计报告 §7（14 条未知项）的可执行分派版。三个执行人道完全并行，互不阻塞。

优先级口径：🔴 卡住后继阶段 ｜ 🟡 影响设计决策 ｜ ⚪ 查漏补缺

---

## 一、给 Weifeng（对外求证与决策，无人可代）

| ID | 事项 | 优先 | 为何重要 | 求证方式 |  |
| --- | --- | --- | --- | --- | --- |
| W1 | 畅读 `projectType` 除 `2` 之外的取值及含义；是否存在小说/漫画品类接口 | 🔴 | 唯一能一句话解锁整条渠道路径的问题 | 书面问畅读对接人；若要试探非 2 取值，**只得用沙箱/测试账号，禁用生产凭证** | 存在 |
| W2 | 接口是否返回**可展示的章节正文文本**（字段名、单次返回几章、有无截断/水印/频率限制） | 🔴 | 对应 R8。不返回正文则 §3.4 站内展示方案整体作废 | 同 W1，一并问 | 可以 |
| W3 | 北斗是否有小说品类接口 | 🔴 | 仓库内北斗 API 零品类参数，完全无从推断 | 书面问北斗对接人 | 可以 |
| W4 | Q4a 试读额度口径：前 N 章 / 前 N 字？是否按渠道、按单本可配？ | 🔴 | **直接卡住 `chapter` 三表设计**，而三表必须在 P1 完成 | 你拍板（可参考 W2 结果后定） | 可以取前100章 |
| W5 | 渠道是否可开独立子账号 / 独立凭证 | 🟡 | R7：与 CPS 共用账号会互相影响配额与换证 | 商务渠道 | 可以。但是建议可以放一起，似乎没有配额的说法，收入也可以通过网文和小说的标签接口区分。 |
| W6 | Q1 域名与品牌：独立还是与现有短剧站共品牌 | 🟡 | 影响 `SITE_URL`、hreflang、IndexNow key、nginx 配置 | 你拍板 | 域名独立，品牌可以不用独立。这个我和运营商量下 |
| W7 | Q3 一期是否做站内搜索（仅书名/作者/题材） | ⚪ | 决定是否一期就上 `pg_trgm` | 你拍板 | 二期 |
| W8 | Q6/Q7 服务器：复用现有 VPS 还是新开；生产库与分析库是否 Day 0 分开；审计数据 T+1 是否可接受 | 🟡 | 定下才能写 compose 与备份 SOP（§3.5） | 你拍板 | 新开vps
是，要新开两个vps实例吗？
可以接受。
尽量可以手动实时同步，同cps。 |
| W9 | Q5 是否抽公共包供 CPS 反向复用 | ⚪ | 审计已给否定意见（抽包必然要动 CPS 生产代码），建议结为「暂不」 | 你确认即可关题 | 暂不 |
| W10 | 向运维索取 CPS 生产库只读统计：`dramas` / `articles` / `cps_tracking_events` 实际行数与库体量 | ⚪ | 新项目容量规划的参照系 | 只读统计，不需导数据 | 好 |
| W11 | 生产实际运行 commit 与审计基线 `cdfb75e` 的差异 | ⚪ | 本基线含未上线的 v8.0 收藏功能，结论略超前于生产 | 读生产 `/api/health` 返回的 commit 比对（只读） | 可以 |

---

## 二、给 Claude Code（CPS 仓库只读，可立即开跑）

统一约束：基线仍为 `cdfb75e`；全程只读，不 `checkout` / 不 `build` / 不 `prisma generate` / 不连生产 / 不读密文值；证据一律 `文件:行号`；报告落 `cps项目/docs/audit/` 下，不污染 CPS 仓库。

| ID | 事项 | 优先 | 产出 |
| --- | --- | --- | --- |
| C1 | `scripts/` 108 个脚本的**全量** SQLite 方言扇扫（上轮只抽样，已知 4 处） | 🟡 | 逐处 `文件:行号`  • 方言类型，补全 PG 迁移工量 |
| C2 | `tests/` 275 个测试中直接依赖 SQLite 行为（非仅依赖测试库文件）的清单 | ⚪ | 测试套件迁移工量 |
| C3 | 5 个「通用」worker handler（`indexnow-delivery` / `sitemap-refresh` / `review-generation` / `retag` / `sync-article-tags`）的短剧耦合度逐文件精读 | 🟡 | 上轮按文件名归为可搬，未逐个验证；`review-generation.ts` 已知有 PG 阻塞点 |
| C4 | 通用底座 8–10 个模块的**精确行数 + import 依赖闭包** | 🟡 | 直接定下「拷哪些文件」的边界，避免拷一个模块牵出半个仓库 |
| C5 | 15 处原生 SQL 的返回类型逐点清单（`COUNT(*)` / BigInt 未转换点） | 🟡 | PG 下 `bigint` 经 Prisma raw 返回 `BigInt`，与 `number` 混算直接 `TypeError` |
| C6 | `favorites/storage.ts` 全文：收藏功能的存储落点（未见对应 Prisma model，疑为客户端存储） | ⚪ | 小说站书架是核心需求，需知道能不能搬 |
| C7 | `.env.example`（8,334 字节）key 名全量对照，补齐仅在 compose/文档出现、代码未引用的 key | ⚪ | **读之前先确认该文件不含真实密文** |
| C8 | `proxy.ts` 未登记进 `PROTECTED_PREFIXES` 的 `(admin)` 路由实际行为：404 还是被当前台公开 | 🔴 | 直接决定新项目鉴权默认值是 fail-open 还是 fail-closed。**本地 dev server 新建一个未登记测试路由即可，禁在生产验** |

---

## 三、给 Codex（新项目侧原型与验证，与 CPS 零交集）

这一道全部在**空白本地环境**做，不碰 CPS 仓库、不碰任何生产资源，因此可与 Gate 0 完全并行。

| ID | 事项 | 优先 | 验收 |
| --- | --- | --- | --- |
| X1 | PG 是否接受 `AT` 作未加引号表别名 | ⚪ | 空白本地 PG 跑 `SELECT 1 FROM (VALUES(1)) at;`，零风险，一分钟出结果 |
| X2 | `novel` / `novel_chapter` / `novel_chapter_content` 三表 schema 草案 + **cursor 分页契约**原型 | 🔴 | 任何返回章节的接口强制 `take`  • cursor，不提供「返回全部」选项。**等 W4 定了额度口径才能定稿，但骨架可先走** |
| X3 | PG 索引草案：含 `promo_code` 索引、partial unique index（`WHERE deleted_at IS NULL`） | 🟡 | 还掉 CPS `schema.prisma:539-542` 记录的欠债；`promo_code` 第一天建索引（CPS 至今无） |
| X4 | worker 原子领取原型：`UPDATE ... RETURNING` vs `SELECT ... FOR UPDATE SKIP LOCKED` 对比 + 多进程并发压测 | 🔴 | 审计硬前置第 2 条：必须在 P2 早期一次做对，之后再改代价降升 |
| X5 | cron 外置方案选型：独立 scheduler 进程 vs worker 内单例 + PG advisory lock | 🟡 | CPS 的 4 个 cron 靠进程内 `globalThis` 去重，web 扩 N 副本就跑 N 次 |
| X6 | `pg_dump`  • WAL 归档 + PITR 的备份与**恢复演练脚本**，并计时 | 🟡 | R5：没演练过的备份等于没备份；P1 门禁项 |
| X7 | 权限三分的 PG 角色脉本（应用 / 只读分析 / 迁移）+ 分析库每日恢复脚本 | 🟡 | 对应 R13 与 §3.5 结论三 |
| X8 | jsonb + GIN 的候选列清单与收益验证（对照 CPS 约 25 个 `*Json String` 列） | ⚪ | 确认哪些真需要索引，不要无差别全换 jsonb |
| X9 | 试读正文防抓取：速率限制 + 访客标识校验原型 | ⚪ | R9。依赖 W2 结果，但方案可先做 |
| X10 | `pg_trgm` vs `tsvector` 对书名/作者/题材检索的对比 | ⚪ | 仅当 W7 = 一期做搜索时开工 |

---

## 四、还需要与你讨论（尚无结论，也还不到开工时机）

这几条不适合交给 AI 调研，需要你的业务判断：

1. **D1 一期上多少本书、多少个语种？** 这直接决定量级设计与工期。CPS 的参照是 14k 行 dramas；如果小说站一期只上 500–1000 本单语种，很多东西（物化视图、只读副本、PgBouncer）都可以明确不做。
答：一期上畅读名下moboreels完整的渠道。
2. **D2 若渠道不给正文，是否接受人工/半自动录入试读章节？** 与 W2 强耦合。这是 R8 的退路问题：不接受则站内展示方案作废、回到跳转渠道试读；接受则多一条内容生产流程与人力成本。
答：若不给正文，一期先不考虑人工。但是目前这个问题解决了。
3. **D3 首页与聚合页形态：**榜单口径是什么（新书/热榜/完结）？是否复用 CPS 的 `home_carousel_*` 5 张表（人工位 + 自动打分 + 合并，B/M）？
答：复用。
4. **D4 埋点是否一期就开？** PG 下已解封（CPS 是被 SQLite 逼得关掉的），但开了就要同时定下保留期、汇总策略、人机识别口径，否则只是换个库累垃圾数据。
答：可以，一期展开
5. **D5 CPS 八期什么时候收口？** 对应 R6。立项书已写「八期收口后再进 P2」，但没写日期，这是工期表的唯一硬约束。
答：不用理这个约束。当初定这个是避免重复造轮子，想等cps短剧八期完成后直接搬到cps小说来，现在8.1.1已经准备发布了。没必要无限期等下去，cps总归要有短剧和小说并行迭代的一天。
6. **D6 工期与人力。** 审计已给出 A/B/C 三分与 S/M/L 工量口径，可直接汇总；但汇总前需要 D1 与 Gate 0 结论，否则只是编数字。建议放到 v0.3 施工版一起做。

---

## 五、依赖关系（哪些事卡哪些事）

```
W1/W2/W3（Gate 0）──┬─→ 能不能立项进 P1（无接口则方案重估，R1）
                      └─→ W2 → D2 → 站内展示方案是否成立（R8）

W4（试读额度）──────→ X2 chapter 三表定稿 ──→ P1 可开工

D1（体量）──────────→ X3 索引草案 / X8 jsonb 范围 / D6 工期

W8（服务器与库隔离）─→ X6 备份恢复 / X7 角色脉本 ─→ P1 门禁

C1–C8 与 X1/X4/X5 完全并行，不依赖任何决策，可立即开跑
```

🔄 **上图与「最小开工集 = W1 + W2 + W4 + W8 + D1」已被推翻（2026-08-01）。** 现行口径：

- **Gate A（P1 本地开工）只差一项**：书量与语种区间（W5 / D1）。其余五条（独立仓库 / PostgreSQL / preview-only materialization / 继承北斗畅读状态机 / 基础数据模型命名）**已全部确认**。
- **W1 不再阻 P1**，只阻 Gate D；**W2 只阻 Gate C**；**W3 不阻任何 Gate**（第二渠道）。
- **W4 不再卡住 `chapter` 三表**：已拍默认值「前 100 章」，且试读策略做成三级可配（全局 → 渠道 → 单本），模型骸架可先定。
- **W8 已结：新开 VPS，Day 0 单实例即可**（PG 与应用同机不同容器，走网络）；审计表与业务表同主库同事务，**不建独立审计库**，分析库后置到阈值触发。
- **W1 / W2 / W3 / W4 目前均为口头结论**，证据等级必须停在 `HYPOTHESIS`；未得书面回复或真实响应之前，不得过 Gate C / Gate D。

| ID | 调研项 | 执行人 | 结果写入 |
| --- | --- | --- | --- |
| W5 | 一期书量、语种、国家区间 | 峰様 | 架构与数据模型字典 |
| C1 | 北斗/畅读架构继承矩阵 | Claude Code | 架构字典 |
| C2 | 可搬模块依赖闭包 | Claude Code | 调研台账、架构字典 |
| I1 | JWT 主体与账户身份 | Codex | 接口台账、字段字典 |
| I2 | 小说品类与项目能力 | Codex＋峰様确认 | 接口台账、字段字典 |
| I3 | 书目列表接口 | Codex | 接口台账、字段字典 |
| I4 | 书籍详情接口 | Codex | 接口台账、字段字典 |
| I5 | 章节目录接口 | Codex | 接口台账、字段字典 |
| I6 | 试读正文接口 | Codex | 接口台账、字段字典 |
| X1 | Novel/Chapter/Source/Preview Schema | Codex | 数据模型字典 |
| X4 | 继承状态机的 PG Worker 原型 | Codex | 架构字典 |
| X5 | Scheduler/Cron 唯一执行方案 | Codex | 架构字典 |

### P1：第一渠道接入前完成

| ID | 调研项 | 执行人 | 结果写入 |
| --- | --- | --- | --- |
| W2 | 正文展示、缓存和授权规则 | 峰様 | 架构字典 |
| W4 | 默认试读额度策略 | 峰様 | 数据模型字典 |
| I7 | 推广链接只读查询 | Codex | 接口及字段字典 |
| I8 | 收益、订单或日报接口 | Codex | 接口及字段字典 |
| I9 | 错误码、限流、重试和幂等 | Codex | 接口及字段字典 |
| I10 | 有副作用接口 Dry-run 摘要 | Codex | 接口台账 |
| C3 | 通用 Worker Handler 耦合审计 | Claude Code | 架构字典 |
| C5 | 小说 ChannelAdapter 契约 | Claude Code | 架构字典 |
| X2 | Cursor 分页契约 | Codex | 架构及数据模型字典 |
| X3 | PostgreSQL 索引方案 | Codex | 数据模型字典 |

### P1 生产部署前完成

| ID | 调研项 | 执行人 | 结果写入 |
| --- | --- | --- | --- |
| W8 | VPS、对象存储、RPO/RTO | 峰様 | 架构字典 |
| X6 | 备份、PITR 和恢复演练 | Codex | 运维架构字典 |
| X7 | 数据库角色和权限 | Codex | 安全架构字典 |
| C4 | 后台鉴权 fail-closed 验证 | Claude Code | 安全架构字典 |
| — | 发布、迁移、回滚 SOP | Cursor/Claude Code | 架构字典 |

## 接口探测必须收集的字段类别

以下只是**候选语义**，不能当作真实字段名。每次 Curl 后必须记录实际字段名、类型和 JSON Path。

### 账户与认证

- 主体 ID、主体类型
- 代理、机构、书城或 App ID
- 权限范围
- Token 到期时间
- JWT 失效和无权限错误
- 是否支持独立子账户

### 书籍

- 渠道书籍 ID
- 标题、别名、英文名
- 作者
- 封面
- 简介
- 分类和标签
- 语言、国家
- 连载或完结状态
- 总章节数
- 免费章节数
- 总字数
- 创建和更新时间
- 上下架状态

### 章节

- 渠道章节 ID
- 所属书籍 ID
- 章节序号
- 标题
- 免费、付费、试读标识
- 正文类型
- 正文内容
- 字数
- 截断标识
- 水印
- 内容哈希
- 更新时间
- 下一页 Cursor

### 推广

- 推广码
- Web URL
- App URL
- Deeplink
- 状态
- 过期时间
- 幂等键
- 是否已经生成
- readback 结果
- 生成失败原因

### 收益

- 统计日期
- 账户
- 书籍
- 推广码
- 点击量
- 激活用户
- 新用户
- 订单数
- 收入
- 币种
- 已结算金额
- 结算状态
- 汇总行标识
- 数据更新时间

### 系统契约

- HTTP 方法
- Host 和 Path
- 必需 Headers
- 分页规则
- 最大 Page Size
- QPS
- 429 行为
- 重试等待
- 幂等行为
- 错误码
- 空数据结构
- 有无副作用
- 可否批量查询
- 可否增量查询

结果反馈：
1、W8：

2、C1+C2

X6+X7

海外小说 CPS P0 第二轮生产只读调研
