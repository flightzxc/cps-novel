# T01–T27 验收矩阵

基线：`daafbe472ef9bfd4810ba0f91451bc9a6eb5e941`。实现分支：`feature/novel-article-decoupling`。

| ID | 断言 | 覆盖 | 状态 |
| --- | --- | --- | --- |
| T01 | 无模板仍可纳入，0 Article | `tests/backend/content-creation/service.test.ts` | 单测 |
| T02 | 已占用 Article slug / 坏模板不挡纳入 | `service.test.ts` | 单测 |
| T03 | 合法重放 already_exists 不补文章、不重入队 Preview | `create-preview-wiring.test.ts` | 单测 |
| T04 | 物化并发：一胜其余冲突/已存在，CAS 失败整笔回滚 | fake CAS + skipIf PG | 单测；PG 待现场 |
| T05 | locale 冲突 / ignored / stale / Novel 软删仍阻断 | `service.test.ts` | 单测 |
| T06 | 缺 Article 不再是 `source_item_inconsistent_state` | `service.test.ts` + `loadPlan` | 单测 |
| T07 | fingerprint / requestToken / scope 纳入新 operation，禁止与旧 `content_create`+模板串用 | `src/lib/tasks/catalog-batch.ts` / `article-generate.ts` | 代码+单测 |
| T08 | DTO 物化成功只含 Novel 字段 | `types.ts` + outcome-copy | 单测 |
| T09 | promo missing / not fetched / blank URL / soft-deleted 可区分 | `generate.test.ts` | 单测 |
| T10 | 执行时重读 PromoLink，不吃旧快照 | `generate.test.ts` | 单测 |
| T11 | 选中的 PromoLink 与 `/go/{code}`、`Article.promoLinkId` 同一行 | `template-rendering.test.ts` | 单测 |
| T12 | 不调用 `bindPromoLinkToArticles` 去重绑旧文 | `generate.ts` 无该调用 | 静态/代码 |
| T13 | 生成读 Novel 当前事实，不读 SourceItem 快照 | `generate.test.ts` | 单测 |
| T14 | 显式坏模板不静默 fallback | `generate.test.ts` | 单测 |
| T15 | 无默认模板 → `template_not_available`，不 `ensureDefaultArticleTemplate` | `template-selection.test.ts` | 单测 |
| T16 | 生成并发：唯一键 + P2002 安全重放 | `generate.test.ts` + skipIf PG | 单测；PG 待现场 |
| T17 | 活文章 → `already_exists`，不走 regenerate | `generate.test.ts` | 单测 |
| T18 | 软删占位 → `article_soft_deleted`，不 undelete | `generate.test.ts` | 单测 |
| T19 | Preview 门槛仍是 ≥1 章（回归，不是 3 章） | 不改 P2-05；`create-preview-wiring.test.ts` | 回归 |
| T20 | 批量发布保持 `no_article`，不后台建稿 | novels 空态 + 既有 batch publish | 回归 |
| T21 | catalog-sync 去掉模板控件；旧 `templateKey` 拒绝 | catalog-sync actions / dialogs | 单测+UI |
| T22 | 旧 `content.create.v1` / 父 `content_create` 终端失败 | handler 单测 + catalog-batch PG | 单测；PG 用例已改 |
| T23 | 文章批 enqueue 的 target 是 Novel，禁止冒充 SourceItem | `article-generate.ts` + actions 单测 | 单测 |
| T24 | 能力位复用 `content:view` / `content:publish`，无新 capability 名 | registry + actions | 单测 |
| T25 | blog / regenerate / 换小说 / 公开 URL / sitemap / IndexNow 不改语义 | 未改这些模块 | 回归 |
| T26 | 真实 `worker_app` / `web_app` 写 article（RETURNING） | skipIf PG | 待现场 |
| T27 | Source → 1 Novel 0 Article → fixture promo 仍 0 → 生成 1 draft 且 bind 正确 | skipIf PG + publish-gate e2e | 核心单测已有；闭环 PG 待现场 |

T26/T27 启用：`NOVEL_ARTICLE_DECOUPLE_DATABASE_TEST=1`，并提供现有 `P1_06_*` 或 catalog-batch 角色 URL。不新增 GRANT。
