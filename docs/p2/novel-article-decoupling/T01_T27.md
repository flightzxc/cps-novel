# T01–T27 验收矩阵

基线：`daafbe472ef9bfd4810ba0f91451bc9a6eb5e941`。
上一轮已审 HEAD：`fbab06e1d31a1b742e2e5d44bf3b2c112a016fcc`。
实现分支：`feature/novel-article-decoupling`。
本轮只追加返修，不 rebase / squash / amend 已审历史。

工单原义 T01–T27 编号不动。返修新断言从 `EXT-01` 起。
每项状态只能是 `PASS` / `FAIL` / `NOT RUN`。

本轮命令（2026-09-14，选 B，不连共享 X8 / UAT / 生产）：

| 命令 | 结果 |
| --- | --- |
| `npm run typecheck` | PASS（0 error） |
| `npm run lint` | PASS（0 error；4 条既有 warning，非本轮引入） |
| `npm test` | PASS：`4881 passed` / `193 skipped` / `0 failed`（含 `tests/ui/admin-secret-boundary.test.tsx`） |
| `NOVEL_ARTICLE_DECOUPLE_DATABASE_TEST=1` 隔离 PG | **NOT RUN**（本轮无独立 volume + 真实 `web_app`/`worker_app`） |
| `LEGACY_TASK_INVENTORY.sql` 在隔离 PG 只读执行 | **NOT RUN** |

本轮无登录浏览器验证 **不是** 复核 §9.3 已满足。Owner UAT 仍欠。

## T01–T27

| ID | 断言 | 文件 | 命令 | 结果 |
| --- | --- | --- | --- | --- |
| T01 | 无模板仍可纳入，0 Article | `tests/backend/content-creation/service.test.ts` | `npm test` | **PASS** |
| T02 | 已占用 Article slug / 坏模板不挡纳入 | `tests/backend/content-creation/service.test.ts` | `npm test` | **PASS** |
| T03 | 合法重放 already_exists 不补文章、不重入队 Preview | `tests/backend/content-creation/create-preview-wiring.test.ts` | `npm test` | **PASS** |
| T04 | 物化并发：一胜其余冲突/已存在，CAS 失败整笔回滚 | `tests/integration/content-creation/novel-article-decouple-postgres.test.ts` | `NOVEL_ARTICLE_DECOUPLE_DATABASE_TEST=1` + 三角色 URL | **NOT RUN**（选 B；具备独立 PG 后切 A 再要求 PASS） |
| T05 | locale 冲突 / ignored / stale / Novel 软删仍阻断 | `tests/backend/content-creation/service.test.ts` | `npm test` | **PASS** |
| T06 | 缺 Article 不再是 `source_item_inconsistent_state` | `tests/backend/content-creation/service.test.ts` | `npm test` | **PASS** |
| T07 | fingerprint / requestToken / scope 纳入新 operation，禁止与旧 `content_create`+模板串用 | `src/lib/tasks/catalog-batch.ts`、`src/lib/tasks/article-generate.ts`、`tests/ui/promo-link-claim-actions.test.ts` | `npm test` | **PASS** |
| T08 | DTO 物化成功只含 Novel 字段 | `src/server/content-creation/types.ts`、catalog-sync outcome-copy | `npm test` | **PASS** |
| T09 | promo missing / not fetched / blank URL / soft-deleted 可区分 | `tests/backend/content-creation/generate.test.ts` | `npm test` | **PASS** |
| T10 | 执行时重读 PromoLink，不吃旧快照 | `tests/backend/content-creation/generate.test.ts` | `npm test` | **PASS** |
| T11 | 选中的 PromoLink 与 `/go/{code}`、`Article.promoLinkId` 同一行 | `tests/backend/content-creation/template-rendering.test.ts` | `npm test` | **PASS** |
| T12 | 不调用 `bindPromoLinkToArticles` 去重绑旧文 | `src/server/content-creation/generate.ts` | 静态：源码无该调用 | **PASS** |
| T13 | 生成读 Novel 当前事实，不读 SourceItem 快照 | `tests/backend/content-creation/generate.test.ts` | `npm test` | **PASS** |
| T14 | 显式坏模板不静默 fallback | `tests/backend/content-creation/generate.test.ts` | `npm test` | **PASS** |
| T15 | 无默认模板 → `template_not_available`，不 `ensureDefaultArticleTemplate` | `tests/backend/content-creation/template-selection.test.ts` | `npm test` | **PASS** |
| T16 | 生成并发：Novel `FOR UPDATE` 后重查；真 P2002 只在事务外收敛 | `tests/integration/content-creation/novel-article-decouple-postgres.test.ts` | `NOVEL_ARTICLE_DECOUPLE_DATABASE_TEST=1` + 三角色 URL | **NOT RUN**（选 B。配套单测 `generate.test.ts` 的锁 / 事务外 winner 已随 `npm test` 绿，但不能代替本项 PG） |
| T17 | 活文章 → `already_exists`，不走 regenerate | `tests/backend/content-creation/generate.test.ts` | `npm test` | **PASS** |
| T18 | 软删占位 → `article_soft_deleted`，不 undelete | `tests/backend/content-creation/generate.test.ts` | `npm test` | **PASS** |
| T19 | Preview 门槛仍是 ≥1 章（回归，不是 3 章） | `tests/backend/content-creation/create-preview-wiring.test.ts` | `npm test` | **PASS** |
| T20 | 批量发布保持 `no_article`，不后台建稿 | novels 空态 + 既有 batch publish | `npm test` | **PASS** |
| T21 | catalog-sync 去掉模板控件；旧 Action 一律 retired，不静默转 Novel-only | `tests/ui/catalog-sync-actions.test.ts`、`tests/ui/promo-link-claim-actions.test.ts`、`tests/ui/batch-create-content-dialog.test.tsx` | `npm test` | **PASS** |
| T22 | 旧 `content.create.v1` / 父 `content_create` 终端失败 | `tests/backend/tasks/novel-article-decouple-handlers.test.ts` | `npm test` | **PASS** |
| T23 | 文章批 enqueue 的 target 是 Novel，禁止冒充 SourceItem | `src/lib/tasks/article-generate.ts`、`tests/ui/article-generate-actions.test.ts` | `npm test` | **PASS** |
| T24 | 能力位复用 `content:view` / `content:publish`，无新 capability 名 | `src/app/api/admin/_lib/registry.ts`、`tests/ui/admin-content-registry.test.ts`、`tests/ui/article-generate-actions.test.ts` | `npm test` | **PASS** |
| T25 | blog / regenerate / 换小说 / 公开 URL / sitemap / IndexNow 不改语义 | 未改这些模块；随全量 `npm test` 回归 | `npm test` | **PASS** |
| T26 | 真实 `worker_app` / `web_app` 写 article（RETURNING）；禁止 owner 顶替 | `tests/integration/content-creation/novel-article-decouple-postgres.test.ts` | `NOVEL_ARTICLE_DECOUPLE_DATABASE_TEST=1` + `P1_06_{OWNER,WEB,WORKER}_DATABASE_URL` | **NOT RUN**（缺任一轮立即 throw；禁止 `DATABASE_URL` / owner 顶替） |
| T27 | Source → 1 Novel 0 Article → fixture promo 仍 0 → 生成 1 draft 且 bind 正确 | 同上隔离 PG harness | 同上 | **NOT RUN** |

T26/T27 启用条件（切 A 之后）：`NOVEL_ARTICLE_DECOUPLE_DATABASE_TEST=1`，且同时提供 `P1_06_OWNER_DATABASE_URL`、`P1_06_WEB_DATABASE_URL`、`P1_06_WORKER_DATABASE_URL`。库名必须匹配 `cps_novel_article_decouple_`。不新增 GRANT。禁止共享 X8 volume / UAT / 生产。

## EXT（本轮返修）

| ID | 断言 | 文件 | 命令 | 结果 |
| --- | --- | --- | --- | --- |
| EXT-01 | loader 返回 `{ pinned, page }`；指定目标不并入候选；失败不 fallback 到 `page[0]` | `src/app/(admin)/articles/generate/_lib/load-generate-page.ts`、`tests/backend/content-creation/article-generate-page.test.ts`、`tests/backend/content-creation/article-generate-eligibility.test.ts` | `npm test` | **PASS** |
| EXT-02 | Client 只从 domain / 同路由 `_lib` 引类型；守卫原样绿 | `src/domain/article-generation.ts`、`tests/ui/admin-secret-boundary.test.tsx` | `npm test` | **PASS** |
| EXT-03 | 「创建文章」→ `/articles/generate`，「批量创建文章」→ `/articles/batch-generate`；空态「去创建第一篇文章」同 route | `tests/ui/articles-admin.test.tsx`、`src/app/(admin)/articles/_components/article-list.tsx` | `npm test` | **PASS** |
| EXT-04 | apply 路径 `SELECT Novel … FOR UPDATE` 后重查再 Prisma create；P2002 禁止在 aborted client 上再查 | `src/server/content-creation/generate.ts`、`tests/backend/content-creation/generate.test.ts` | `npm test` | **PASS**（单测；真实并发见 T16 NOT RUN） |
| EXT-05 | harness 模块顶层不建 PrismaClient、不拼 `DATABASE_URL`；缺 web/worker URL 立即 throw | `tests/integration/content-creation/novel-article-decouple-postgres.test.ts` | `npm test`（flag 关闭整组 skip） | **PASS**（lazy skip）；实跑见 T04/T16/T26/T27 **NOT RUN** |
| EXT-06 | `all_filtered` 只入队父任务 `article.generate.batch.v1`（filter snapshot），Action 不返回数万 ID；leaf ≤200 | `src/lib/tasks/article-generate.ts`、`worker/handlers/article-generate-batch.ts`、`tests/ui/article-generate-actions.test.ts`、`tests/backend/content-creation/article-generation-selection.test.ts` | `npm test` | **PASS** |
| EXT-07 | catalog-batch `linked && novelId` → `alreadyLinkedCount`，与 blocked / failed / ineligible 分开 | `worker/handlers/catalog-batch.ts`、`src/server/catalog-batch/service.ts` | `npm test` | **PASS** |
| EXT-08 | 旧 `dryRun/applyContentCreation(+Batch)Action` 无模板也 `retired_protocol`，不得 enqueue `novel_materialize` | `src/app/(admin)/catalog-sync/_actions.ts`、`tests/ui/catalog-sync-actions.test.ts`、`tests/ui/promo-link-claim-actions.test.ts` | `npm test` | **PASS** |
| EXT-09 | inventory SQL 文本：query 1 不含 `locked_until`/`lease_epoch`；lease 只在 query 3 的 `generic_task_item`；无 UPDATE/DELETE | `docs/p2/novel-article-decoupling/LEGACY_TASK_INVENTORY.sql`、`tests/backend/content-creation/legacy-task-inventory-sql.test.ts` | `npm test` | **PASS** |
| EXT-10 | inventory SQL 在同 schema 隔离 PG 上只读执行，0 SQL error | `docs/p2/novel-article-decoupling/LEGACY_TASK_INVENTORY.sql` | 隔离 PG `psql` | **NOT RUN** |
| EXT-11 | Novel-only 守卫扩到 materialize worker / catalog-batch handler 边界 | `tests/backend/content-creation/materialize-no-article-side-effects.test.ts` | `npm test` | **PASS** |
| EXT-12 | leftover `templateKey` 上 `novel.materialize.v1` 终端 `failed`（`legacy_template_on_materialize`） | `worker/handlers/novel-materialize.ts`、`tests/backend/tasks/novel-article-decouple-handlers.test.ts` | `npm test` | **PASS** |

## 本轮明确未做 / 不得写成已满足

- 未登录 Admin，未代替 Owner 做最终浏览器 UAT（指定 Novel、第 201 本批量可达、跨页选择、catalog-sync 无模板、Novel-only「尚未创建文章」）。
- 未把无登录单测写成复核 §9.3 release gate 已满足。
- 未连共享 X8 volume、UAT、生产；未用 owner URL 冒充应用角色。
- 未把 T04 / T16 / T26 / T27 或 inventory SQL 实跑写成 PASS。
