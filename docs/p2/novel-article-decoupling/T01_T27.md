# T01–T27 验收矩阵

基线：`daafbe472ef9bfd4810ba0f91451bc9a6eb5e941`。
上一轮已审 HEAD：`90f6ab765242200c7c2a9e6877ff1279cbb45db9`。
实现分支：`feature/novel-article-decoupling`。
本轮只追加定点返修，不 rebase / squash / amend 已审历史。
本轮仍是 **CHANGES 返修 / READY_FOR_INCREMENTAL_REVIEW**，不宣布 RELEASE_READY。

T01–T27 **题目恢复为原施工工单第八节 / 终审附录 A 原文**。
1901880 矩阵里被改写的主题改挂 EXT 或「历史编号别名」，不再占用原编号。

每项状态只能是 `PASS` / `FAIL` / `NOT RUN`。
证据类型：`静态` / `单测` / `PG` / `浏览器`。

本轮命令（2026-09-14，选 B，不连共享 X8 / UAT / 生产）：

| 命令 | 结果 |
| --- | --- |
| `npm run typecheck` | **PASS**（0 error） |
| `npm run lint` | **PASS**（0 error；4 条既有 warning，非本轮引入） |
| `npm test` | **4905 passed / 193 skipped / 0 failed**（含 `tests/ui/admin-secret-boundary.test.tsx`） |
| `NOVEL_ARTICLE_DECOUPLE_DATABASE_TEST=1` 隔离 PG | **NOT RUN** |
| `LEGACY_TASK_INVENTORY.sql` 在隔离 PG 只读执行 | **NOT RUN** |

本轮无登录浏览器验证。Owner UAT 仍欠。无登录单测不能写成复核 §9.3 已满足。

## T01–T27（原工单题目）

| ID | 原工单场景 / 期望 | 证据类型 | 文件 | 命令 | 结果 |
| --- | --- | --- | --- | --- | --- |
| T01 | 系统没有适用文章模板，纳入一个合法 SourceItem → 仍可创建 Novel+linked；Article 为 0；不调用模板服务 | 单测 | `tests/backend/content-creation/service.test.ts` | `npm test` | **PASS** |
| T02 | Article.slug 冲突或模板语法损坏 → 不影响 Novel-only 物化；文章问题只能在文章生成阶段出现 | 单测 | `tests/backend/content-creation/service.test.ts` | `npm test` | **PASS** |
| T03 | 同 SourceItem 重复纳入 → 同一本 Novel、无新增 Article、无伪造 articleId | 单测 | `tests/backend/content-creation/service.test.ts`（`already_exists` 重放） | `npm test` | **PASS** |
| T04 | 两个不同请求并发纳入同 SourceItem → 只有一份合法 Novel/link；失败方无孤儿记录，可安全重放 | PG | `tests/integration/content-creation/novel-article-decouple-postgres.test.ts` | `NOVEL_ARTICLE_DECOUPLE_DATABASE_TEST=1` + 三角色 URL | **NOT RUN** |
| T05 | linked 的有效 Novel 没有 Article → 正常已纳入状态，不报 inconsistent；可浏览后台、领取推广 | 单测 | `tests/backend/content-creation/service.test.ts`（缺 Article 不再 `source_item_inconsistent_state`） | `npm test` | **PASS** |
| T06 | linked 指向不存在／已删除／locale 冲突的 Novel → 保留真实错误，不把坏数据当正常重放 | 单测 | `tests/backend/content-creation/service.test.ts`（locale 冲突 / ignored / stale / 软删 Source） | `npm test` | **PASS** |
| T07 | claim 成功时还没有 Article → PromoLink 正常落在 Novel 上；无需文章，不因空绑定集失败 | 单测 | `tests/backend/tasks/promo-link-claim-handler.test.ts`（无 Article 仍可 claim） | `npm test` | **PASS** |
| T08 | 先 claim、后生成 Article → 新文章创建当次即绑定正确 PromoLink；不需要第二次 claim | 单测 | `tests/backend/content-creation/generate.test.ts`（创建即写 `promoLinkId`）；claim handler 后绑补偿 | `npm test` | **PASS** |
| T09 | 没推广、未 fetched、只有空白 URL 或已软删除推广 → 新小说文章生成被阻断，不留下半成品；按既有可用口径区分原因 | 单测 | `tests/backend/content-creation/generate.test.ts` | `npm test` | **PASS** |
| T10-a | 执行时重读 Promo，不吃更早快照 | 单测 | `tests/backend/content-creation/generate.test.ts`（先 not_ready，后 ready 再生成） | `npm test` | **PASS** |
| T10-b | 入队时推广有效、执行前失效 | 单测 | 无「先 ready 再失效」专用用例；不把 T10-a 反向路径冒充本项 | — | **NOT RUN** |
| T11-a | 生成按本书 `novelId` 查 Promo，不跨书 first | 静态 | `src/server/content-creation/promo.ts` `where: { novelId }` | 静态核验 | **PASS** |
| T11-b | 候选推广属于别的 Novel／无法解析的归属歧义 → 明确阻塞 | 单测 | 无错绑/歧义专用用例 | — | **NOT RUN** |
| T12 | 正常 Novel + 推广 + 模板生成 → Draft Article、templateId、promoLinkId、正文／SEO／Schema 与审计一致；不自动发布 | 单测 | `tests/backend/content-creation/generate.test.ts`、`template-rendering.test.ts` | `npm test` | **PASS** |
| T13 | 修改过 Novel 当前标题／简介后再生成 → 使用 Novel 当前事实，不使用旧 SourceItem 覆盖 | 单测 | `tests/backend/content-creation/generate.test.ts` | `npm test` | **PASS** |
| T14 | 模板显式选择错误 locale／类型／停用或损坏 → 明确失败，不静默退回默认；既有 Novel 保留 | 单测 | `tests/backend/content-creation/generate.test.ts` | `npm test` | **PASS** |
| T15 | 不显式选模板 → 仅文章生成阶段使用并显示合法默认模板；不跨语种 fallback | 单测 | `tests/backend/content-creation/template-selection.test.ts` | `npm test` | **PASS** |
| T16 | 两个不同 requestId／不同模板并发生成同书同语种 → 最终仅一篇有效文章；另一请求安全返回已存在或可恢复冲突 | PG | `tests/integration/content-creation/novel-article-decouple-postgres.test.ts` | `NOVEL_ARTICLE_DECOUPLE_DATABASE_TEST=1` + 三角色 URL | **NOT RUN**（配套单测 `generate.test.ts` 锁 / 事务外 winner 已绿，不能代替本项 PG） |
| T17 | 已有人工编辑／已发布／SEO-only／hidden 文章，再点创建 → 不覆盖正文、绑定、身份、状态与可见性；指向既有文章 | 单测 | `tests/backend/content-creation/generate.test.ts`（活文章 `already_exists`） | `npm test` | **PASS** |
| T18-a | 模板渲染失败 → 无半成品 Article | 单测 | `tests/backend/content-creation/template-rendering.test.ts`（`template_render_failed` 且 `articles.size === 0`） | `npm test` | **PASS** |
| T18-b | 短 ID 写入失败 → 无半成品 Article | 单测 | 无 generate 路径「short-id 失败且 0 Article」专用用例 | — | **NOT RUN** |
| T18-c | 数据库唯一冲突无赢家 → 无半成品 Article | 单测 | `tests/backend/content-creation/generate.test.ts`（P2002 无赢家） | `npm test` | **PASS** |
| T19 | 新文章没有完成 Preview → 生成不擅自新增三章等限制；发布仍按现有 gate 拒绝不合格状态 | 静态 + 单测 | `src/server/content-creation/generate.ts` 不创建章节；`tests/backend/publish-gate/evaluator.test.ts`、`tests/backend/content-creation/publish-gate-e2e.test.ts`（`preview_chapter_missing`） | `npm test` | **PASS**（**不**引用 `create-preview-wiring.test.ts`：那份只测物化后 Preview 入队去重，不是「≥1 章」门槛） |
| T20 | Novel-only 单／批量发布 → 明确「尚未创建文章」；不触发默认模板自动建稿 | 单测 | `tests/ui/novel-publish-lifecycle-panel.test.tsx`、`tests/ui/novels-batch-publish.test.tsx` | `npm test` | **PASS** |
| T21 | 新任务相同请求重放／配置不一致 → 正确去重；配置不一致报 mismatch；不串用旧协议 fingerprint | 单测 | `tests/backend/tasks/article-generate-factory.test.ts`；`src/lib/tasks/article-generate.ts` / `catalog-batch.ts` | `npm test` | **PASS** |
| T22 | 旧 v1 创建任务／旧父任务／旧页面提交 → 明确不再执行旧隐式建稿；不静默转换；历史任务详情仍可读 | 单测 | `tests/backend/tasks/novel-article-decouple-handlers.test.ts`、`tests/ui/catalog-sync-actions.test.ts`、`tests/ui/promo-link-claim-actions.test.ts` | `npm test` | **PASS** |
| T23 | 单条、批量、部分失败、跨语言 → 同核心规则、正确逐项结果，保留已有目录筛选全选能力 | 单测 | `tests/ui/article-generate-actions.test.ts`、`tests/backend/content-creation/article-generation-selection.test.ts`、`tests/ui/article-batch-generate-form.test.tsx` | `npm test` | **PASS** |
| T24-a | Preview 入队 / 去重 token | 单测 | `tests/backend/content-creation/preview-enqueue.test.ts` | `npm test` | **PASS** |
| T24-b | Preview 任务重试 | 单测 | 无 Preview 专用 retry 用例 | — | **NOT RUN** |
| T24-c | 失去 lease 的执行者不能提交业务写入 | 单测 | `tests/backend/tasks/db-retry-wiring.test.ts`（GenericTask fencing；非 Preview 专测） | `npm test` | **PASS**（平台 fencing；Preview 专测 **未单列**） |
| T25 | blog、重新生成、换小说、公开 URL、sitemap、IndexNow → 本轮不改变既有行为，相关回归通过 | 单测 | 未改这些模块；随全量 `npm test` 回归 | `npm test` | **PASS** |
| T26 | 真实应用／worker DB 角色 → 不依靠 owner/superuser 或仅 mock 权限通过集成测试 | PG | `tests/integration/content-creation/novel-article-decouple-postgres.test.ts` | `NOVEL_ARTICLE_DECOUPLE_DATABASE_TEST=1` + `P1_06_{OWNER,WEB,WORKER}_DATABASE_URL` | **NOT RUN** |
| T27 | 安全回退演练／书面边界 → 新版产生 Novel-only 后不会通过回退恢复隐式建稿或把合法数据判损坏 | 静态 | `docs/p2/novel-article-decoupling/SWITCH_ROLLBACK.md` 已写边界；演练未做 | 书面核验 | **NOT RUN**（书面已交，演练未做；不把书面当演练 PASS） |

T26/T27 启用条件（切 A 之后）：`NOVEL_ARTICLE_DECOUPLE_DATABASE_TEST=1`，且同时提供 `P1_06_OWNER_DATABASE_URL`、`P1_06_WEB_DATABASE_URL`、`P1_06_WORKER_DATABASE_URL`。库名必须匹配 `cps_novel_article_decouple_`，三连接 `current_database()` 必须相同。不新增 GRANT。禁止共享 X8 volume / UAT / 生产。禁止 owner 顶替应用角色。

## 历史编号别名（1901880 矩阵曾改写的主题）

这些断言本身仍有效，但**不得再占用原工单编号**。

| 1901880 曾用编号 | 当时被改写成的主题 | 现挂 |
| --- | --- | --- |
| T07 | fingerprint / requestToken / 禁止与旧 `content_create` 串用 | T21 + EXT-13 |
| T08 | DTO 物化成功只含 Novel 字段 | EXT-14 |
| T11 | 选中的 PromoLink 与 `/go/{code}`、`Article.promoLinkId` 同一行 | T08 / T12 + EXT-17 |
| T18 | 软删占位 → `article_soft_deleted`，不 undelete | T17 + EXT-15 |
| T24 | 能力位复用 `content:view` / `content:publish` | EXT-16 |
| T27 | Source → 1 Novel 0 Article → fixture promo 仍 0 → 生成 1 draft | T12 的隔离 PG 实跑；见 EXT-18，**NOT RUN** |

## EXT

| ID | 断言 | 证据类型 | 文件 | 命令 | 结果 |
| --- | --- | --- | --- | --- | --- |
| EXT-01 | loader 返回 `{ pinned, page }`；指定目标不并入候选；失败不 fallback 到 `page[0]` | 单测 | `src/app/(admin)/articles/generate/_lib/load-generate-page.ts`、`tests/backend/content-creation/article-generate-page.test.ts` | `npm test` | **PASS** |
| EXT-02 | Client 可从 domain / 同路由 `_lib` 引类型与 `normalizeArticleGenerateFilter`；不得引 `@/server` / credentials；守卫原样绿 | 单测 | `src/domain/article-generation.ts`、`tests/ui/admin-secret-boundary.test.tsx` | `npm test` | **PASS** |
| EXT-03 | 「创建文章」→ `/articles/generate`，「批量创建文章」→ `/articles/batch-generate` | 单测 | `tests/ui/articles-admin.test.tsx` | `npm test` | **PASS** |
| EXT-04 | apply 路径 `SELECT Novel … FOR UPDATE` 后重查再 Prisma create；P2002 禁止在 aborted client 上再查 | 单测 | `src/server/content-creation/generate.ts`、`tests/backend/content-creation/generate.test.ts` | `npm test` | **PASS**（真实并发见 T16 **NOT RUN**） |
| EXT-05 | harness 模块顶层不建 PrismaClient、不拼 `DATABASE_URL`；缺 web/worker URL 立即 throw | 单测 | `tests/integration/content-creation/novel-article-decouple-postgres.test.ts` | `npm test`（flag 关闭整组 skip） | **PASS**（lazy skip）；实跑见 T04/T16/T26/T27 **NOT RUN** |
| EXT-06 | `all_filtered` 只入队父任务；handler 对 201+ 合格 Novel 拆出每片 ≤200 的 leaf | 单测（假数据 handler 分页，**不是 PG**） | `worker/handlers/article-generate-batch.ts`、`tests/backend/tasks/novel-article-decouple-handlers.test.ts` | `npm test` | **PASS**（假数据；**不**用 Action mock 充当全链路；**不是**隔离 PG 证据） |
| EXT-07 | catalog-batch `linked && novelId` → `alreadyLinkedCount` | 单测 | `worker/handlers/catalog-batch.ts` | `npm test` | **PASS** |
| EXT-08 | 旧 `dryRun/applyContentCreation(+Batch)Action` 无模板也 `retired_protocol` | 单测 | `tests/ui/catalog-sync-actions.test.ts`、`tests/ui/promo-link-claim-actions.test.ts` | `npm test` | **PASS** |
| EXT-09 | inventory SQL 文本：query 1 不含 item-lease 列；lease 只在 query 3；无 UPDATE/DELETE | 单测 | `docs/p2/novel-article-decoupling/LEGACY_TASK_INVENTORY.sql`、`tests/backend/content-creation/legacy-task-inventory-sql.test.ts` | `npm test` | **PASS** |
| EXT-10 | inventory SQL 在同 schema 隔离 PG 上只读执行，0 SQL error | PG | `docs/p2/novel-article-decoupling/LEGACY_TASK_INVENTORY.sql` | 隔离 PG `psql` | **NOT RUN** |
| EXT-11 | Novel-only 守卫扩到 materialize worker / catalog-batch handler 边界 | 单测 | `tests/backend/content-creation/materialize-no-article-side-effects.test.ts` | `npm test` | **PASS** |
| EXT-12 | leftover `templateKey` 上 `novel.materialize.v1` 终端 `failed` | 单测 | `tests/backend/tasks/novel-article-decouple-handlers.test.ts` | `npm test` | **PASS** |
| EXT-13 | fingerprint / requestToken / scope 纳入新 operation，禁止与旧 `content_create`+模板串用 | 单测 | `src/lib/tasks/catalog-batch.ts`、`src/lib/tasks/article-generate.ts`、`tests/backend/tasks/article-generate-factory.test.ts` | `npm test` | **PASS** |
| EXT-14 | DTO 物化成功只含 Novel 字段 | 静态 + 单测 | `src/server/content-creation/types.ts`、catalog-sync outcome-copy | `npm test` | **PASS** |
| EXT-15 | 软删占位 → `article_soft_deleted`，不 undelete | 单测 | `tests/backend/content-creation/generate.test.ts` | `npm test` | **PASS** |
| EXT-16 | 能力位复用 `content:view` / `content:publish`，无新 capability 名 | 单测 | `src/app/api/admin/_lib/registry.ts`、`tests/ui/admin-content-registry.test.ts`、`tests/ui/article-generate-actions.test.ts` | `npm test` | **PASS** |
| EXT-17 | 选中的 PromoLink 与 `/go/{code}`、`Article.promoLinkId` 同一行 | 单测 | `tests/backend/content-creation/template-rendering.test.ts`、`generate.test.ts` | `npm test` | **PASS** |
| EXT-18 | Source → 1 Novel 0 Article → fixture promo 仍 0 → 生成 1 draft 且 bind 正确 | PG | 隔离 PG harness | 同 T26 条件 | **NOT RUN** |
| EXT-19 | `article.generate.batch.v1` 接入 list/detail/progress；父枚举完成 + children pending 不得 `completed`；零匹配不是成功 1 本 | 单测 | `tests/backend/task-admin/article-generate-parent-aggregation.test.ts`、`tests/ui/admin-task-detail-page.test.tsx` | `npm test` | **PASS** |
| EXT-20 | 批量 form：`draftFilter` / `appliedFilter` 分离；未应用禁用提交；未知结果重放冻结快照；成功后换 requestId | 单测 | `tests/ui/article-batch-generate-form.test.tsx` | `npm test` | **PASS** |
| EXT-21 | 页面出现尚未缓存的 locale 时补取该语种模板并合并 cache | 单测 | `tests/ui/article-batch-generate-form.test.tsx`、`tests/ui/article-generate-actions.test.ts` | `npm test` | **PASS** |
| EXT-22 | harness 三连接隔离库名检查且 `current_database()` 必须同名；负例单测不连库 | 单测 | `tests/backend/content-creation/novel-article-decouple-database-guard.test.ts` | `npm test` | **PASS** |
| EXT-23 | 应用筛选只生成一份 canonical filter；list / total / paging / all_filtered / fingerprint 共用 | 单测 | `src/domain/article-generation.ts`、`tests/ui/article-batch-generate-form.test.tsx`、`tests/ui/article-generate-actions.test.ts` | `npm test` | **PASS** |

## 本轮明确未做 / 不得写成已满足

- 未登录 Admin，未代替 Owner 做最终浏览器 UAT。
- 未把无登录单测写成复核 §9.3 release gate 已满足。
- 未连共享 X8 volume、UAT、生产；未用 owner URL 冒充应用角色。
- 未把 T04 / T16 / T26 / T27 或 inventory SQL 实跑写成 PASS。
- 未做 N1（>200 显式拆叶文案）、N2（alreadyLinked「已纳入」口径）、N3 checkpoint。
- 未宣布 `1901880` 或本轮 NEW_HEAD 为增量 PASS / RELEASE_READY。

## 附录 A：原施工工单 T01–T27 原文（W01）

取自 `CPS_Haiyue_Novel_Article_Decoupling_Work_Order_2026-09-14.md` 第八节。
源文件 SHA-256：`72ac1ce306dfa42db85bdf6e25875cffbf876846245c62335a941f3c063a1e60`。

附录保留原措辞，避免第三轮再漂编号。它不是声称所有项已经执行通过。

| ID | 场景 | 期望 |
|---|---|---|
| T01 | 系统没有适用文章模板，纳入一个合法 SourceItem | 仍可创建 Novel+linked；Article 为 0；不调用模板服务 |
| T02 | Article.slug 冲突或模板语法损坏 | 不影响 Novel-only 物化；文章问题只能在文章生成阶段出现 |
| T03 | 同 SourceItem 重复纳入 | 同一本 Novel、无新增 Article、无伪造 articleId |
| T04 | 两个不同请求并发纳入同 SourceItem | 只有一份合法 Novel/link；失败方无孤儿记录，可安全重放 |
| T05 | linked 的有效 Novel 没有 Article | 正常已纳入状态，不报 inconsistent；可浏览后台、领取推广 |
| T06 | linked 指向不存在／已删除／locale 冲突的 Novel | 保留真实错误，不把坏数据当正常重放 |
| T07 | claim 成功时还没有 Article | PromoLink 正常落在 Novel 上；无需文章，不因空绑定集失败 |
| T08 | 先 claim、后生成 Article | 新文章创建当次即绑定正确 PromoLink；不需要第二次 claim |
| T09 | 没推广、未 fetched、只有空白 URL 或已软删除推广 | 新小说文章生成被阻断，不留下半成品；按既有可用口径区分原因 |
| T10 | 入队时推广有效，执行前失效 | 执行时重新判断并阻断，不盲信 UI／入队快照 |
| T11 | 候选推广属于别的 Novel／错误来源，或存在无法解析的归属歧义 | 不误绑定、不随便 first；返回明确阻塞 |
| T12 | 正常 Novel + 推广 + 模板生成 | Draft Article、templateId、promoLinkId、正文／SEO／Schema 与审计一致；不自动发布 |
| T13 | 修改过 Novel 当前标题／简介后再生成 | 使用 Novel 当前事实，不使用旧 SourceItem 覆盖 |
| T14 | 模板显式选择错误 locale／类型／停用或损坏 | 明确失败，不静默退回默认；既有 Novel 保留 |
| T15 | 不显式选模板 | 仅文章生成阶段使用并显示合法默认模板；不跨语种 fallback |
| T16 | 两个不同 requestId／不同模板并发生成同书同语种 | 最终仅一篇有效文章；另一请求安全返回已存在或可恢复冲突 |
| T17 | 已有人工编辑／已发布／SEO-only／hidden 文章，再点创建 | 不覆盖正文、绑定、身份、状态与可见性；指向既有文章 |
| T18 | 渲染／短 ID／数据库写入失败 | 无半成品 Article；先前 Novel 与合法 PromoLink 不受损 |
| T19 | 新文章没有完成 Preview | 生成不擅自新增三章等限制；发布仍按现有 gate 拒绝不合格状态 |
| T20 | Novel-only 单／批量发布 | 明确“尚未创建文章”；不触发默认模板自动建稿 |
| T21 | 新任务相同请求重放／配置不一致 | 正确去重；配置不一致报 mismatch；不串用旧协议 fingerprint |
| T22 | 旧 v1 创建任务／旧父任务／旧页面提交 | 明确不再执行旧隐式建稿；不静默转换；历史任务详情仍可读 |
| T23 | 单条、批量、部分失败、跨语言 | 同核心规则、正确逐项结果，保留已有目录筛选全选能力 |
| T24 | Preview 入队、重试、lease 丢失 | 保留既有调度／去重／fencing；失去 lease 的执行者不能提交业务写入 |
| T25 | blog、重新生成、换小说、公开 URL、sitemap、IndexNow | 本轮不改变既有行为，相关回归通过 |
| T26 | 真实应用／worker DB 角色 | 不依靠 owner/superuser 或仅 mock 权限通过集成测试 |
| T27 | 安全回退演练／书面边界 | 新版产生 Novel-only 后不会通过回退恢复隐式建稿或把合法数据判损坏 |
