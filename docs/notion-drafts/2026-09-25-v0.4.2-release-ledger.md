# 海阅 版本管理与发版手账 —— v0.4.2 发版记录（Notion 草稿镜像）

> 本文件是 Notion 手账页的仓库内镜像（CPS 惯例 `docs/notion-drafts/`）。本会话没有 Notion 连接器，由 Owner 把
> 交接提示词转给 ChatGPT 写入。两边不一致时以已核实的仓库记录为准。

## 版本登记台账（本次新增行，加在 v0.4.1 之上）

| 版本号 | 日期(+0800) | Bump | 摘要 | Commit / Release | 状态 |
| --- | --- | --- | --- | --- | --- |
| v0.4.2 | 2026-09-25 03:57 | PATCH | 领推广按接口分别限速（开关默认关闭）+ 目录页位置登记（含数据库迁移，不改执行行为） | tag v0.4.2 → 33cd67bd34c34af1d3dbcbf78e6f2636d175c889；镜像 cps-novel:0.4.2-33cd67b | 预生产已发布（按接口限速开关关闭）；生产未上线 |

## 本次发版详细记录

- 发布日期（+0800）：2026-09-25 03:57（部署 03:56:57–03:57:35）
- 环境：预生产（www.bangbangji.cloud / 后台 zbcwf.bangbangji.cloud）
- 版本号与 Bump：v0.4.2，PATCH
- Git：Final SHA 33cd67bd34c34af1d3dbcbf78e6f2636d175c889；annotated tag v0.4.2；开发线 integration/v0.4.2-2026-09-25（基于 v0.4.1 收官提交 a2e08a9）
- 合入：feat/moboreader-per-endpoint-rate-limit @ ce9fdc0（领推广正式修复第 4 阶段·按接口限速）、feat/catalog-position-registration @ e2c9002（第 5 阶段·目录页位置登记）+ 版本身份升到 0.4.2
- 镜像与发布身份：cps-novel:0.4.2-33cd67b（linux/amd64），config digest sha256:e8659dacfb8a7ab14aaa7ae3e989b7efbf5d9ac9db9e74d92e9142d1dc5c5037，归档 sha256 802a331c00eabf20172184f8152078108fec353020ccb57714053abaa0a2fbca，发布目录 /opt/cps-novel/releases/33cd67bd34c34af1d3dbcbf78e6f2636d175c889
- 为什么发这一版：第二级验收实测领取约 790 本 / 小时，原因是本地一道 1.5 秒的全局闸门让三类上游调用排同一队，而上游其实按接口分别计数。照此速度剩余约 7.6 万本要跑 96 小时以上，渠道令牌 10-01 到期。Owner 批准在正式领取前先提速，并同意两部分一起发版。
- 已上线：目录扫描时登记每本书在目录中的页码位置（新数据库字段）；领取分片按页排序，为下一步"按页预读"做准备。本版不改变任何上游调用行为。
- 已合入但开关默认关闭：按接口分别限速（领取前后的目录查询与领取各自计时，另有主机级最小间隔和余量保护）。开关关闭时与旧版完全一样。
- 进行中：开启按接口限速（先各 1.5 秒跑 24 小时，再降到 1.2 秒）与全量目录扫描登记页码，均待 Owner 另行批准；按页预读（第 5 阶段第二步）待施工；随后第三级正式领取剩余约 7.6 万本。
- 主要问题与处理：复核中发现并修正上游限流响应头的时间含义理解错误；外部审阅后又修三处真缺陷（上游要求的冷却时间被截短、等待结束后没再检查任务是否已中止、本机时钟偏差）；组装发版时发现新配置没有传进 worker 容器（发版检查会显示已开启、容器里其实是关的），已补上并加自动测试。
- Owner 关键决策：2026-09-24 批准第 4、5 阶段设计（证据时效 3 分钟、批准页排序稳定性探测）；2026-09-25 同意两部分一起发版；2026-09-25 授权 v0.4.2 部署（仅限预生产主机），部署后两个新能力保持关闭。
- 数据库变化：新增迁移 20260924090000_p5a_catalog_position（书目表加一个可空字段，不改已有数据），权限无变化。配置变化：APP_VERSION 0.4.1→0.4.2、NEXT_PUBLIC_BUILD_VERSION v0.4.1→v0.4.2（备份 preprod.env.bak-20260924T195612Z）；未写入任何新的限速配置；生命周期开关保持开启。
- 发布前备份：cps-novel-20260924T195132Z.dump（校验通过）。
- 验收结果：质量门禁通过（类型检查、生产构建、字典漂移 0、四组真实数据库集成测试全过；全量测试 6,672 通过，2 条已知的满载超时单独重跑均通过，与本版无关）；本机与目标机双重核对归档校验和、源码提交、amd64 架构；发布脚本全部步骤通过，RELEASE=PASS；部署后核对健康接口、版本身份、限速开关确为关闭、迁移已生效、后台与公开站域名隔离正常、无错误日志。
- 回滚：回滚到 v0.4.1（0a25469）兼容，不需要撤销数据库迁移，两个版本变量改回 0.4.1 即可。
- 仍未解决：开启按接口限速与全量目录扫描（待批准）；按页预读待施工；第三级正式领取；B-1 / B-2 / B-3 / B-5 / B-6 待办；预生产备份服务停止；nginx 429 与 sudo 项；61 条人工核对；公开站点地图为空（既有情况）；Notion 页面链接待回填。
