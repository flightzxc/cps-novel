# 海阅 版本管理与发版手账 —— v0.4.3 发版记录（Notion 草稿镜像）

> 本文件是[Notion 手账页](https://app.notion.com/p/3e4601b5fd3481b5a39bcf48408015c2)的仓库内镜像。
> v0.4.3 已直接同步并读回核对；两边不一致时以已核实的仓库记录为准。待完成项不得写成已验收。

## 版本登记台账（本次新增行，加在 v0.4.2 之上）

| 版本号 | 日期(+0800) | Bump | 摘要 | Commit / Release | 状态 |
| --- | --- | --- | --- | --- | --- |
| v0.4.3 | 2026-09-25 22:35 | PATCH | admin2 独立后台身份与双 UUID 领取白名单；分片枚举真实库 helper 修复；无迁移或 grants 变更 | tag v0.4.3 → 505feeaae04ae1a23df3e7f6a27795f4128636f3；镜像 cps-novel:0.4.3-505feea | 预生产已发布；admin2 2FA 与双账号认证核验待完成；生产未上线 |

## 本次发版详细记录

- 发布日期（+0800）：2026-09-25 22:35
- 环境：预生产 `haiyue-vps`（公开站 www.bangbangji.cloud / 后台 zbcwf.bangbangji.cloud）；生产未上线
- 版本号与 Bump：v0.4.3，PATCH
- Git：Final SHA `505feeaae04ae1a23df3e7f6a27795f4128636f3`；annotated tag `v0.4.3`；开发线 `integration/v0.4.3-2026-09-25`（基于 v0.4.2 收官提交 `e1e9396`）
- 合入：已获 Opus 复核的 admin2 一次性建号命令与运维手册（`f036cc4`），分片枚举真实库用例 helper 规划器修复（`4329620`），预生产模板中的 admin/admin2 双 UUID 白名单示例，以及 0.4.3 版本身份
- 镜像与发布身份：`cps-novel:0.4.3-505feea`（linux/amd64）；config digest `sha256:e601019cf1009564496deba6d5952cd2d64c4cff21c17b63cbd6f483d10a9f31`；归档 sha256 `fecbd3eaee7063a3f128cd02c08e55217e159a31cc56be4a5e1e6d888bfb02e5`；发布目录 `/opt/cps-novel/releases/505feeaae04ae1a23df3e7f6a27795f4128636f3`
- 为什么发这一版：Owner 需要 admin2 与 admin 同密码、同权限、独立 2FA；正式领取批次运行期间部署会重启 worker，故 Owner 先暂停批次 `eba8f359-a569-43d7-bb55-b71fecc02f6e`，再授权第二阶段发布
- 已完成：只读核实批次暂停、处理中分片条目 0、非终态 promo claim 意图 0；在线逻辑备份；传输并校验 git bundle、镜像归档和 manifest；发布脚本 `RELEASE=PASS`；admin2 建号预演、正式创建、回放；白名单追加 admin2 identity ID 后只重建 web
- admin2：identity ID `c5fd40e2-6f1d-4543-8251-27fb5ac941ac`；独立 super_admin 身份，初始无 2FA、恢复码和会话记录；admin 原 2FA 密文摘要建号前后不变。Owner 现场绑定 admin2 的独立 2FA 和离线保存恢复码尚未完成
- 领取白名单：`PROMO_CLAIM_USER_IDS` 从 `71f0e655-0640-4f07-bcbb-041f027fa7cb` 变为 `71f0e655-0640-4f07-bcbb-041f027fa7cb,c5fd40e2-6f1d-4543-8251-27fb5ac941ac`；`PROMO_CLAIM_ROLES` 保持空；web 单独重建，worker/scheduler 容器 ID、Created、镜像逐项不变
- Owner 关键决策：只走已发布版本的路径 A；暂停正式领取批次后部署；admin2 沿用 admin 当前密码但重新加盐；高风险 promo claim 仍按账号 UUID 白名单授权；本次不恢复批次、不做真实领取提交验收，也不改文章生成与 sitemap 配置
- 数据库与配置：无新迁移或 grants 变更。先仅改 `APP_VERSION` 0.4.2→0.4.3、`NEXT_PUBLIC_BUILD_VERSION` v0.4.2→v0.4.3（备份 `preprod.env.bak-20260925T143330Z`）；建号后再追加白名单 UUID（备份 `preprod.env.bak-20260925T144425Z`）。worker 的按接口限速开关仍 true，两接口间隔仍各 1,500 ms，领推广生命周期开关仍 true
- 发布前备份：`cps-novel-20260925T142332Z.dump`，190,409,997 字节，sha256 `45676de334f0d932a50fcea284e0c3071b36c44ae2a734278bca1c3a3fa881c9`，`LOGICAL_BACKUP=PASS`
- 验收结果：本地 `tsc`、生产构建、五组真实 PostgreSQL 验证运行器通过；全量 `npm test` 为 446 文件通过 / 32 跳过、6,692 测试通过 / 314 跳过。目标机归档与源码身份、linux/amd64 核对通过；`RELEASE_EXIT=0`；health 0.4.3 / Final commit / metadata passed / database passed；后台与公开站路由隔离正常，三服务近 5 分钟错误日志为 0；postgres 未重建
- 回滚：v0.4.2 Final `33cd67bd34c34af1d3dbcbf78e6f2636d175c889` 与当前数据库结构兼容；账号与白名单为独立运维状态，回滚前按手册评估
- 仍未完成：Owner 绑定 admin2 2FA 后，分别以 admin2/admin 执行 `verify-admin-auth.ts` 并复核 admin 原 2FA 密文摘要；正式领取批次由 Owner 自行恢复；真实 promo claim 提交验收须等批次结束，且每次由 Owner 单独授权
