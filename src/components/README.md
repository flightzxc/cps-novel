# src/components/

**Owner: Claude（独占写入）**

## 用途

跨页面复用的 UI 基元与组合组件：通用列表/表单/弹窗基元、按钮、卡片、导航等。供 `src/app/`、`src/features/admin-ui/`、`src/features/public-ui/` 消费。

## 本轮范围

🔴 本轮（P1-04）只建目录，不写实现。当前目录仅含本 README 与 `.gitkeep` 占位。

## 填充任务

随 **P1-09（后台框架、菜单、字段与 CPS UI 复刻）** 与 **P1-10（用户端深色设计系统和页面壳）** 填充。
