# src/design/

**Owner: Claude（独占写入）**

## 用途

设计系统层：`--novel-*` / `--reader-*` 设计 token 定义、深色主题色值、对比度基线。是用户端视觉体系的唯一真源，不与后台浅色主题混用。

## 本轮范围

🔴 本轮（P1-04）只建目录，不写实现。当前目录仅含本 README 与 `.gitkeep` 占位。

## 填充任务

由 **P1-10（用户端深色设计系统和页面壳）** 填充。
