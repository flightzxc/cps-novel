# tests/integration/

**Owner: Codex（独占写入）**

## 用途

跨模块集成测试：前后端契约联调验证、Compose 环境下的端到端链路测试、SOL 5.6 审计阶段补充验证用例。

## 本轮范围

🔴 本轮（P1-04）只建目录，不写实现。当前目录仅含本 README 与 `.gitkeep` 占位。

## 填充任务

由 **P1-13（测试、项目隔离检查、恢复 smoke test）** 填充；P1-14 审计阶段可在此补充验证用例。

## 特别纪律

- 恢复 smoke test 必须实际跑通并计时；
- 集成测试环境不得引用 CPS 只读参考路径。
